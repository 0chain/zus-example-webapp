version: '3.2'

services:

  webapp:
    container_name: zus-webapp
    build:
      context: ../
      dockerfile: Dockerfile-static
    restart: unless-stopped
    ports:
      - 3007:80
    networks:
      - proxy-tier

networks:
  proxy-tier:
    driver: bridge